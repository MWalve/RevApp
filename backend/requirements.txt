# FastAPI and Server
fastapi==0.141.1
uvicorn[standard]==0.53.0
pydantic==2.13.5
pydantic-settings==2.15.0

# LangChain Core (pinned to the 0.3.x line - rag_system.py uses the
# legacy RetrievalQA/PromptTemplate/langchain_community.llms.Ollama
# APIs that were replaced in langchain 1.x; upgrading past 0.3.x
# requires rewriting rag_system.py against the new LCEL-based APIs)
langchain==0.3.30
langchain-community==0.3.31
langchain-core==0.3.86

# Vector Store and Embeddings
langchain-chroma==0.2.6
langchain-huggingface==0.3.1
chromadb==1.5.9
sentence-transformers==6.1.0

# LLM Providers (Ollama is default, others optional)
# Ollama (no package needed, uses REST API)

# Optional: For Groq integration
langchain-groq==0.3.8

# Document Loading
pypdf==6.19.0

# ML/AI Libraries
torch==2.14.0
transformers==5.17.0

# Utilities
python-dotenv==1.2.3
